#ifdef ONLINE_JUDGE
#include <bits/stdc++.h>
#define debug(...) 17
#else
#include "debug.h"
#endif
using namespace std;

#define fi first
#define se second
using str = string;
using ll = long long;
using ld = long double;
using pll = pair<ll, ll>;
using pld = pair<ld, ld>;
using pii = pair<int, int>;

template <class T> T read_() {
  T x;
  cin >> x;
  return x;
}
#define gint read_<int>()
#define gstr read_<str>()
#define rpt(i, u, v) for (ll i = (u), _n_ = (v); i < _n_; i++)
#define tpr(i, u, v) for (ll i = (u), _n_ = (v); --i >= _n_;)

template <class T> using maxHeap = priority_queue<T, vector<T>>;
template <class T> using minHeap = priority_queue<T, vector<T>, greater<T>>;
template <class A, class B> bool chmax(A &a, const B b) {
  return a < b && (a = b, 1);
}
template <class A, class B> bool chmin(A &a, const B b) {
  return b < a && (a = b, 1);
}
const int mod = 1000000007;
const ld eps = 0.000000001, pi = acos(-1);
const ll e30 = 1ll << 30, e60 = 1ll << 60;

namespace config {
const bool MULTI_TESTS = 1;
const bool IS_INTERACTIVE = 0;
const int FLOAT_PRECISION = 0;
} // namespace config

namespace me {
auto stay_strong() {

}
} // namespace me

int main() {
  if (!config::IS_INTERACTIVE)
    cin.tie(nullptr)->sync_with_stdio(false);
  if (config::FLOAT_PRECISION)
    cout << fixed << setprecision(config::FLOAT_PRECISION);
  int days_without_you = 1;
  if (config::MULTI_TESTS)
    cin >> days_without_you;
  while (days_without_you--)
    me::stay_strong();
}

/* -------
    Life can't be this beautiful
        without your presence! ✨
*/
